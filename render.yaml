services:
  - type: web
    name: tudulu-api
    env: node
    region: frankfurt
    plan: free
    rootDir: apps/api
    buildCommand: npm install --prefix ../.. --legacy-peer-deps && npx prisma generate && npm run build
    startCommand: node dist/main
    envVars:
      - key: NODE_ENV
        value: production
